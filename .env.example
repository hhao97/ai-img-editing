# ===================================
# AI 电商图片生成器 - 环境变量配置
# ===================================
# 请复制此文件为 .env 并填写真实的配置值
# ===================================

# ===================================
# 应用基础配置
# ===================================
# Manus 应用 ID（如果使用 Manus 平台）
VITE_APP_ID=your-app-id-here

# Node 环境（development / production）
NODE_ENV=development

# 服务器端口（可选，默认 3000）
PORT=3000

# ===================================
# Supabase 数据库配置
# ===================================
# Supabase 项目的数据库连接字符串
# 格式：postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# Supabase 项目 URL（用于 Better Auth）
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anon Key（公开的 API 密钥）
SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key（服务端密钥，请勿泄露）
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ===================================
# Better Auth 认证配置
# ===================================
# JWT 签名密钥（生成方式：openssl rand -base64 32）
BETTER_AUTH_SECRET=your-better-auth-secret-key-here

# 应用基础 URL（用于 OAuth 回调）
BETTER_AUTH_BASE_URL=http://localhost:3000

# 允许的认证方式（可选配置，如：email,google,github）
BETTER_AUTH_PROVIDERS=email

# Google OAuth（如需使用 Google 登录）
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth（如需使用 GitHub 登录）
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ===================================
# OpenRouter API 配置
# ===================================
# OpenRouter API Key（用户需要自行提供，可在前端输入）
# 如需服务端统一配置，可在此设置默认值
OPENROUTER_API_KEY=your-openrouter-api-key

# ===================================
# 文件存储配置
# ===================================
# 存储方式选择：supabase / s3 / forge
STORAGE_PROVIDER=supabase

# --- Supabase Storage 配置 ---
# Supabase Storage Bucket 名称
SUPABASE_STORAGE_BUCKET=images

# --- AWS S3 配置（如使用 S3）---
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# --- Manus Forge Storage 配置（如使用 Forge）---
BUILT_IN_FORGE_API_URL=https://forge.manus.app/
BUILT_IN_FORGE_API_KEY=your-forge-api-key

# ===================================
# 管理员配置
# ===================================
# 项目所有者的用户 ID（用于权限管理）
OWNER_USER_ID=your-owner-user-id

# ===================================
# OAuth 配置（如果使用 Manus OAuth，可保留）
# ===================================
# Manus OAuth 服务器地址
OAUTH_SERVER_URL=https://oauth.manus.app

# ===================================
# 其他配置
# ===================================
# 日志级别（debug / info / warn / error）
LOG_LEVEL=info

# 是否启用 API 调用日志
ENABLE_API_LOGGING=true

# ===================================
# 注意事项
# ===================================
# 1. .env 文件不应提交到 Git 仓库（已在 .gitignore 中）
# 2. 生产环境请使用强密码和真实密钥
# 3. JWT_SECRET 应使用至少 32 字符的随机字符串
# 4. Supabase 数据库连接字符串可在 Supabase Dashboard > Settings > Database 中找到
# 5. OpenRouter API Key 可在 https://openrouter.ai 注册获取
# 6. Better Auth 文档：https://better-auth.com
# ===================================
